# Line segment

`CreateLineSeg(ID: string; sp, tp: TST3DPoint)` — create a line segment from two points.

- `ID` — identifier of the entity;
- `sp` — start point;
- `tp` — end point.

![CreateLineSeg](images/curve-lineseg.svg)
