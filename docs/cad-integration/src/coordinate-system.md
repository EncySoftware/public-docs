# Coordinate system

`CreateCoordinateSystem(CSID: string; vT, vZ, vX: TST3DPoint)` — create a coordinate system. It is defined in absolute coordinates.

- `CSID` — coordinate system identifier;
- `vT` — position (origin);
- `vZ` — Z axis;
- `vX` — X axis.
