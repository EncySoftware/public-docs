# Inverted curve

`CreateInversedCurve(ID: string; SourceCurveID: string)` — create a curve with reversed (inverted) parameterization: the traversal direction of the source curve is reversed.

- `ID` — identifier of the entity to create;
- `SourceCurveID` — identifier of the source curve.
